# {{ ansible_managed }}
127.0.0.1   localhost.localdomain localhost
::1         localhost6.localdomain6 localhost6

{{ main_ip }} zabbix

{% for item in groups['agent'] %}
{{ hostvars[item]['ansible_default_ipv4']['address'] }} {{ hostvars[item]['ansible_fqdn']}} 
{% endfor %}
